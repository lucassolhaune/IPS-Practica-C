#include <stdio.h>
#include <stdlib.h>
#include <malloc.h>

int **expandir(int **matriz, int filas, int cols);

int main() {

	int **matriz;
	int **matriz2;
	int filas, columnas;

	matriz = (int **)malloc(sizeof(int *));
	matriz2 = (int **)malloc(sizeof(int *));


	return 0;
}
